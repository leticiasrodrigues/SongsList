<ul>
  <% @album.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
</ul>

<%= form_for @album do |f| %>
	<%= f.label :name, 'Name' %>
	<%= f.text_field :name %>
  <%= f.label :year, 'Year' %>
  <%= f.text_field :year %>
  <%= f.submit 'Create' %>
<% end %>
